!!! Для изменения параметров подключения соединения с базой данных нужно использовать файл documents/config.txt
!!! В архиве документов изначально лежит дефолтный конфиг (+ инициализация, если случайно удалили)

!!! Для корректной работы веб-приложения нужно настроить конфигурационные файлы apache и php (инструкция server-configuration)
!!! Доступы указаны в конце инструкции server-configuration

!!! Расширение mysqli, использующееся в PHP 7, пока не поддерживает caching_sha2_password аутентификацию, поэтому для работы с mysql 8.0
(для версий ниже 8.0 ничего исправлять не нужно) для пользователя, с которым будет работать скрипт, 
нужно выполнить команду (в mysql клиенте):

ALTER USER 'mysqlUsername'@'localhost' IDENTIFIED WITH mysql_native_password BY 'mysqlUsernamePassword';


Также можно в my.ini поправить default_authentication_plugin на mysql_native_password (но должно помочь и первое).

!!! Используется только фул-дамп таблиц, т.к. импорт дампа Базы данных может породить конфликт привилегий
!!! Для импорта дампа, нужно создать у себя базу данных (имя не имеет значение) и через phpMyAdmin (идет в архиве в директории documents)
импортировать sql-дамп в созданную базу данных